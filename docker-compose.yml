version: '3.8'

services:
  server:
    build: 
      context: .
      dockerfile: server/Dockerfile
    image: server-todo-app-image
    container_name: server
    ports:
      - "8080:8080"
    volumes:
      - ./server:/app
      - ./server/database:/app/database
    command: air